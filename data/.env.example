# Allium secrets template — copy to data/.env locally.
# NEVER commit data/.env.

ALLIUM_PASSWORD=CHANGE_ME_LOCALLY
# Optional future/online credentials belong here, not in data/config.json.
# ALLIUM_API_KEY=
# ALLIUM_TOKEN=
